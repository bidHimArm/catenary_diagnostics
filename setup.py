from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='An approach for OCS (Overhead Catenary System) diagnostics based on the contact force measurements',
    author='Femto-ST',
    license='MIT',
)
